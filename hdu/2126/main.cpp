#include <iostream>

using namespace std;

int a[1001];
int b[2000000];
int len;

int main()
{
    int n, m, h, k;

    while(scanf("%d%d%d%d", &n, &m, &h, &k) != EOF){
        for(int i = 0; i < n; i++){
            scanf("%d", &a[i]);
        }
        len = 0;
        for(int i = n - 1; i >= 1; i--){

        }
    }
    return 0;
}
