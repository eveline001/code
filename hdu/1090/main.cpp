#include <iostream>

using namespace std;

int main()
{
    int T, a, b;
    cin >> T;
    for(int cas = 1; cas <= T; cas++){
        cin >> a >> b;
        cout << a + b << endl;
    }
    return 0;
}
